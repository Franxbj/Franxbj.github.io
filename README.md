# How to Code a Sandcastle - HTML School Project

## 📚 About This Project
This is a school assignment where I created a single HTML webpage showcasing my favorite book: **"How to Code a Sandcastle"** by Josh Funk. The project demonstrates the use of essential HTML elements, tags, and attributes.

## 📖 Book Information
- **Title:** How to Code a Sandcastle
- **Author:** Josh Funk
- **Genre:** Children's Picture Book, Educational, STEM/Coding
- **Year Released:** 2018
- **Awards:** Children's Book Council STEM Book List

## 🎯 Project Requirements
This project includes the following HTML elements:

### 1. HTML Boilerplate
- `<!DOCTYPE html>`
- `<html>`, `<head>`, `<body>`
- `<title>` tag

### 2. Header Section
- `<h1>` to `<h6>` headings
- `<div>` for grouping content

### 3. Introduction Paragraph
- `<p>` for text content
- `<em>` for italics
- `<strong>` for emphasis
- `<br>` for line breaks
- `<span>` for highlighting
- `id` attribute for internal linking

### 4. Image Section
- `<img>` tag with `src`, `alt`, and `title` attributes

### 5. Lists
- `<ul>` for unordered list (characters)
- `<ol>` for ordered list (memorable scenes)
- `<li>` for list items

### 6. Video Section
- `<iframe>` for embedded YouTube video

### 7. Links
- `<a>` tags with `href`, `title`, `target` attributes
- External links (`target="_blank"`)
- Internal links (`href="#sectionID"`)

### 8. Table
- `<table>`, `<thead>`, `<tbody>`, `<tfoot>`
- `<tr>`, `<th>`, `<td>`
- `colspan` and `rowspan` for merged cells
- `scope` attribute for accessibility

### 9. Comments
- Multiple HTML comments throughout the code

## 🚀 How to View
1. Clone this repository
2. Open `index.html` in your web browser

## 📝 Notes
This is a beginner HTML project created as part of my web development coursework. The focus is on proper HTML structure and using a variety of HTML elements correctly.

## 📧 Contact
[Francis Boafo/Franxbj]

---

**Assignment completed:** [03/11/2025]
